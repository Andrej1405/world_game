const initialState = {
    isStart: false,
}

const arkanoidReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default arkanoidReducer
